You are an expert in the structure of the Babylonian Talmud.

ATTACHED SHARED FILES

1. OrHaSugya_OutputSchema.json
2. OrHaSugya_AnalysisStandards.json

Treat both shared files as binding instructions.

The Output Schema is authoritative for the complete JSON structure.
The Analysis Standards file is authoritative for controlled vocabulary, scoring,
conditional-value handling, filenames, and global analysis rules.

Do not redefine either shared file inside this prompt.
Do not omit any field required for this stage.
When a field is conditional or not applicable, preserve it and use the empty value
specified by the shared schema and standards.

Return one valid JSON object and no prose outside the JSON.

EXACT FILE CONTRACT

Prompt filename:
sugyaAnalysisPrompt.txt

Required shared input filenames:
OrHaSugya_OutputSchema.json
OrHaSugya_AnalysisStandards.json

Required stage input filenames:
<TRACTATE>_bundle.jsonl

Required output filename:
<TRACTATE>_sugya_analysis.json

These names are binding. Replace <TRACTATE> with the tractate name used by the project.
Do not add version numbers, dates, descriptors, or suffixes to any filename.


STAGE

Sugya Boundary and Discourse-Structure Analysis

INPUT

<TRACTATE>_bundle.jsonl

OUTPUT

<TRACTATE>_sugya_analysis.json

Populate only:

outputs.sugya_analysis

from OrHaSugya_OutputSchema.json. Return that object as the root output object;
do not wrap it in the top-level schema document.

OBJECTIVE

Identify the natural beginning and end of every sugya in the tractate.
Follow continuity of argument rather than changes in topic, speaker, Mishnah,
or cited source.

ANALYSIS RULES

1. Track questions, answers, challenges, proofs, objections, distinctions,
   alternative explanations, resolutions, narratives, applications, and resumptions.

2. Declare a new sugya only when the Gemara begins a genuinely self-contained
   discussion. When two sections remain logically dependent, keep them together.

3. Treat an interruption followed by return as one sugya containing a digression.
   Record the digression and resume information using every applicable schema field.

4. For each boundary, identify the concrete evidence:
   new Mishnah, new legal question, formal transition, change in argumentative task,
   closure of the prior investigation, embedded narrative, return to an earlier
   discussion, or another textually supported signal.

5. Record all primary verses and rabbis materially involved in the sugya.
   Do not treat every mentioned name as primary.

6. Give a concise title, purpose, structure description, confidence score,
   boundary reason, and start snippet as required by the schema.

7. After the first pass, perform a complete second pass over the tractate.
   Check for false splits, missed transitions, embedded digressions, resumptions,
   and invalid daf/segment locations. Populate the second-pass validation fields.

8. Accuracy is more important than producing small units. Prefer a slightly broader
   sugya over an unsupported split.

FINAL CHECK

Verify that:
- every daf/segment location exists in the bundle;
- sugyot cover the intended tractate sequence without accidental gaps or overlaps;
- all conditional digression fields remain present;
- the root object exactly matches the sugya_analysis schema.
